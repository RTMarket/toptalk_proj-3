---
description: 高级聊天室消息有效期 + 后来者可见性 + 文件/图片下载权限规则已锁定，改动前须征得用户同意
globs: src/pages/PremiumChatRoom.tsx,src/lib/premiumRoomDbMessages.ts,src/lib/premiumRoomStorage.ts
alwaysApply: true
---

# 高级聊天室消息有效期 / 后来者可见 / 下载权限 — 已锁定

用户已确认：高级聊天室中消息支持“阅后即焚有效期”（发送时由用户选择）。**后来加入房间的用户**应遵守同一条消息在发送时设定的有效期与下载权限。以下视为**稳定区**。

## 规则（必须保持一致）

1. **后来者可见性（时间规则）**
   - 用户进入高级房间后，应能看到**仍在有效期内**（未到期）的历史消息（包括文字/文件/图片）。
   - 已到期焚毁的消息：后来者**看不到**（历史拉取时应过滤；UI 不得“补回”已过期内容）。

2. **文件/图片下载权限（allowDownload）**
   - 发送者在发送文件/图片时选择的 `allowDownload`，对后来者同样生效：
     - 若 `allowDownload=true`：后来者在消息消失前可点击下载。
     - 若 `allowDownload=false`：后来者在消息消失前也**不能**下载。
   - 权限不得因为“后来者”身份而改变。

## 锁定范围（未经用户明确同意禁止修改）

- `src/pages/PremiumChatRoom.tsx`
  - 进房拉取历史消息（“只拉取未过期消息”）逻辑
  - 消息有效期倒计时、到期隐藏/清除逻辑
  - 文件/图片消息渲染与下载按钮的 `allowDownload` 判定
- `src/lib/premiumRoomDbMessages.ts`
  - `expireAt` 的计算、历史消息过滤（到期返回 null/不入列表）
  - 文件消息元数据解析（含 `allowDownload`）

## 必须遵守

1. **任何**对上述文件/逻辑的改动，须先向用户说明：**为什么要改、改什么、风险是什么（尤其是对后来者可见性与下载权限的影响）**，并得到**明确同意**后再动。
2. 禁止为了“代码整洁/性能优化”改变过滤顺序、时钟口径或下载权限判定。

## 例外（仍需谨慎）

- 仅修复**明确 bug**（且可复现）时，可最小范围修改，并在回复中**逐条说明**改了哪些行、为何不影响上述规则。

